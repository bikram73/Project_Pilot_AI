# Lemma API endpoint configuration
LEMMA_API_URL="https://api.lemma.work"

# Your custom workspace Pod Identifier
LEMMA_POD_ID=""

# The unique key of the Project Analyzer workflow
LEMMA_WORKFLOW="project-analysis-workflow"

# Your secret SDK authorization Bearer token (Keep this private on backend only)
LEMMA_SESSION_TOKEN=""

# Allowed Origin for CORS proxy config
LEMMA_PROXY_ALLOWED_ORIGIN="http://localhost:5173"

# APP_URL: The URL where this applet is hosted
APP_URL="MY_APP_URL"

# Google Gemini API Key (Secret, only accessed on the backend)
GEMINI_API_KEY=""
